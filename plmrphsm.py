from random import choice


#fro the lib function  crate a class university include a funct lib ()
#issue the lib book by talking the reg no as an argrument ..create a new lib func that can take name and dob as an argument
class University:
    def library(self,issue):
        self.issue=issue
        choice=input("enter reg no.")
        if choice==("yes"):
            print("y can take the book")
        else:
            print("no u can't take the book")
        def library():
             print(function)
        def libraryu vvmnm
            print(choice)
University.library("yes")
University.library("no")





